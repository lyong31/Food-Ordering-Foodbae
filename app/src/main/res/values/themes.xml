<resources>
    <!-- Base application theme. -->
    <style name="Theme.Foodbae" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/color_primary</item>
        <item name="colorPrimaryDark">@color/color_primary</item>
        <item name="colorAccent">@color/teal_200</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:fontFamily">@font/bodoni</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="TransparentStatusBar" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorPrimary">@color/color_primary</item>
        <item name="android:fontFamily">@font/bodoni</item>
        <item name="colorPrimaryDark">@android:color/transparent</item>

        <!-- Status bar color. -->
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowTranslucentStatus">true</item>
    </style>

    <style name="Theme.Foodbae.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.Foodbae.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.Foodbae.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>